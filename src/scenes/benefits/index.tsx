import {
  HomeModernIcon,
  UserGroupIcon,
  AcademicCapIcon,
} from "@heroicons/react/24/solid";
import { SelectedPage } from "../../shared/types";

type Props = { setSelectedPage: (value: SelectedPage) => void };

const Benefits = ({ setSelctedPage }: Props) => {
  return <div></div>;
};

export default Benefits;
